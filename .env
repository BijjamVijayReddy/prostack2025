# Local development — points to your locally running backend
NEXT_PUBLIC_API_URL=https://prostack-admin.onrender.com

# Show "Extend session?" dialog this many ms before token expiry
# 60000 = 1 minute before expiry (production); use 30000 for testing
NEXT_PUBLIC_SESSION_WARN_BEFORE_MS=60000
